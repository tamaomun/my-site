---
interface Props {
  image: string;
  caption: string;
  showCaption?: boolean;
}

const {
  image,
  caption,
  showCaption = true
} = Astro.props;
---

<li>
  <img src={image} alt={caption} />

  {showCaption && (
    <p class="illust-caption">{caption}</p>
  )}
</li>